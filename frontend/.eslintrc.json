{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "Property[key.name='onMutate']",
        "message": "Optimistic updates are disabled (Requirement 4.8). Use useConfirmedMutation and invalidate in onSuccess."
      }
    ]
  }
}
