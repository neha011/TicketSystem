# jqwik defaults for the whole test suite.
#
# Tries are deliberately low to keep `mvn test` fast during development. Individual
# @Property methods can raise this with `@Property(tries = ...)` when a property's
# input space genuinely needs more coverage than the default.
jqwik.tries.default=25
