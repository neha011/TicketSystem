/**
 * Custom Bean Validation constraints. Notably the trimmed-length constraint, which measures
 * text after stripping whitespace so a whitespace-only value counts as zero-length.
 */
package com.ticketsystem.ticket.validation;
