/**
 * HTTP boundary: request mapping, validation triggering, status codes, and response headers.
 * Contains no business rules — those live in {@code com.ticketsystem.ticket.service}.
 */
package com.ticketsystem.ticket.controller;
