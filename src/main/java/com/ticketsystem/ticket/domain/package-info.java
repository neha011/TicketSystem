/**
 * JPA entities and domain enums, including the ticket status allowed-transition table.
 * Entities never leave the service layer — controllers exchange DTOs exclusively.
 */
package com.ticketsystem.ticket.domain;
