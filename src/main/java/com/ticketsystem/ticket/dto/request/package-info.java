/**
 * Inbound request payloads carrying their validation constraints, so a request is rejected at
 * the controller boundary before any service method runs.
 */
package com.ticketsystem.ticket.dto.request;
