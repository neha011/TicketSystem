/**
 * Request and response payload types plus explicit entity/DTO mappers. Keeps JPA concerns
 * such as {@code @Version} and lazy proxies out of the wire format.
 */
package com.ticketsystem.ticket.dto;
